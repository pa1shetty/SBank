package com.example.sbank.utils

class EnumClasses {
    enum class ButtonState {
        DISABLE,
        ENABLE
    }

    enum class FocusState {
        FOCUS_DAY,
        FOCUS_MONTH,
        FOCUS_YEAR
    }

    enum class DataType {
        PAN,
        DAY,
        MONTH,
        YEAR
    }
}